// App Version Header File for iFAST
#pragma once

// Note:  The .CPP file is generated by pid_brander.exe during a pre-build phase

#include <dstring.hpp>

#undef IFASTCBO_LINKAGE
#ifdef IFASTCBO_DLL
   #define IFASTCBO_LINKAGE CLASS_EXPORT
#else
   #define IFASTCBO_LINKAGE CLASS_IMPORT
#endif


namespace ifds
{
   // Get the current application version label
   IFASTCBO_LINKAGE const DString& getAppVersion();

   // Get the current application build number.  If this is 0 then the build is local (i.e. not a scripted build)
   IFASTCBO_LINKAGE unsigned long getAppBuildNumber();

   // Get the name of the computer which was used to build the application
   IFASTCBO_LINKAGE const DString& getAppBuildComputer();

   IFASTCBO_LINKAGE struct IfdsVersionInfo
   {
      DString versionMajor;
      DString versionMinor;
      boolean isQA;
      //format 
      // - 16.3.0.x     for QA 
      // - 16.3.x       for UAT
      // - iFAST 16.2.3 for Base
      IfdsVersionInfo(DString versionIn);
   private:
      IfdsVersionInfo();
   };
}



