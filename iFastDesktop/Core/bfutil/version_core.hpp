// Core Version Header File for iFAST
#pragma once

// Note:  The .CPP file is generated by pid_brander.exe during a pre-build phase

#include <dstring.hpp>

#undef BFUTIL_LINKAGE
#ifdef BFUTIL_DLL
   #define BFUTIL_LINKAGE CLASS_EXPORT
#else
   #define BFUTIL_LINKAGE CLASS_IMPORT
#endif

namespace ifds
{
   // Get the current Corelication version label
   BFUTIL_LINKAGE const DString& getCoreVersion();

   // Get the current Corelication build number.  If this is 0 then the build is local (i.e. not a scripted build)
   BFUTIL_LINKAGE unsigned long getCoreBuildNumber();

   // Get the name of the computer which was used to build the Corelication
   BFUTIL_LINKAGE const DString& getCoreBuildComputer();
};



